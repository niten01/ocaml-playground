name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    name: build

    strategy:
      fail-fast: false
      matrix:
        include:
          - { ocaml-compiler: 5.3.0, os: ubuntu-latest }
          - { ocaml-compiler: 5.3.0, os: windows-latest }
          - { ocaml-compiler: 5.1.0, os: ubuntu-latest }
          - { ocaml-compiler: 5.1.0, os: windows-latest }

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          submodules: true

      - name: Re-synchronize the package index files on Ubuntu
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update

      - name: Use OCaml ${{ matrix.ocaml-compiler }}
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{ matrix.ocaml-compiler }}

      - run: opam install . --deps-only

      - run: opam exec -- dune build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with: 
          name: build-${{ matrix.ocaml-compiler }}-${{ matrix.os }}
          path: _build
          retention-days: 5
